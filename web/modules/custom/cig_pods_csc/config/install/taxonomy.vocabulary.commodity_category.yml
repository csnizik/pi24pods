langcode: en
status: true
dependencies:
 enforced:
    module:
      - cig_pods_csc
name: 'Commodity category'
vid: commodity_category
description: 'Category of commodity(ies) produced in field enrolled in the project'
weight: 0
